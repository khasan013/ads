import {getUser} from '../services/storage'; import {History as Icon} from 'lucide-react'; export default function History(){const u=getUser();return <section className="page"><span className="eyebrow">YOUR ACTIVITY</span><h1>Transaction history</h1><p className="lead">A local record of your demo points and unlocked rewards.</p><div className="table-card">{u.transactions.length?<table><thead><tr><th>Activity</th><th>Points</th><th>Date</th></tr></thead><tbody>{u.transactions.map(t=><tr key={t.id}><td>{t.activity}</td><td className={t.points>0?'positive':'negative'}>{t.points>0?'+':''}{t.points}</td><td>{new Date(t.date).toLocaleString()}</td></tr>)}</tbody></table>:<div className="empty"><Icon/><h2>Nothing here yet</h2><p>Your completed demo experiences will appear here.</p></div>}</div></section>}
