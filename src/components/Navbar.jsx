import {Link,NavLink,useNavigate} from 'react-router-dom'; import {Menu,X,LogOut,Sparkles} from 'lucide-react'; import {useState} from 'react';
export default function Navbar({authenticated,signOut}){const [open,setOpen]=useState(false), nav=useNavigate(); const links=authenticated?[['Dashboard','/dashboard'],['Rewards','/rewards'],['History','/history'],['Profile','/profile']]:[['Home','/'],['Rewards','/rewards'],['How it works','/#how'],['About','/about']]; const leave=()=>{signOut();nav('/')};return <header><nav className="nav"><Link className="brand" to="/"><Sparkles/> Luma</Link><div className="desktop-links">{links.map(([x,p])=><NavLink key={x} to={p}>{x}</NavLink>)}</div><div className="nav-action">{authenticated?<button className="text-btn" onClick={leave}><LogOut size={17}/> Logout</button>:<Link className="btn small" to="/login">Sign in</Link>}<button className="menu" aria-label="Toggle navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-menu">{links.map(([x,p])=><NavLink onClick={()=>setOpen(false)} key={x} to={p}>{x}</NavLink>)}{authenticated&&<button onClick={leave}>Logout</button>}</div>}</nav></header>}
