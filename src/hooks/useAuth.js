import {useState} from 'react'; import {currentUser,isAuthed,login,logout} from '../services/auth'; export function useAuth(){const [user,setUser]=useState(currentUser()); const [authenticated,setAuthenticated]=useState(isAuthed()); return {user,authenticated,signIn:(u,p)=>{const next=login(u,p);setUser(next);setAuthenticated(true)},signOut:()=>{logout();setAuthenticated(false)}}}
