import {motion} from 'framer-motion'; import {X,Play,CheckCircle} from 'lucide-react'; import {useState} from 'react'; import {showRewardedAd} from '../services/rewardProvider';
export default function RewardModal({onClose,onComplete}){const [started,setStarted]=useState(false),[left,setLeft]=useState(15),[done,setDone]=useState(false);const start=()=>{setStarted(true);showRewardedAd({onProgress:setLeft,onRewardEarned:r=>{onComplete(r);setDone(true)}})};return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-labelledby="reward-title"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="modal"><button aria-label="Close" className="close" onClick={onClose}><X/></button><span className="eyebrow">DEMO MODE</span>{done?<><CheckCircle className="success"/><h2 id="reward-title">Reward completed!</h2><p>+10 demo points have been added to your balance.</p><button className="btn full" onClick={onClose}>Continue</button></>:<><div className="play-circle"><Play fill="currentColor"/></div><h2 id="reward-title">Rewarded Ad Demo</h2><p>Watch the sponsored experience to receive your platform reward. This is a simulated experience—not a live advertisement.</p>{started?<div className="count"><b>{left}s</b><span>Experience in progress</span></div>:<button className="btn full" onClick={start}>Start demo experience</button>}<small>The reward unlocks after the countdown completes.</small></>}</motion.div></div>}
